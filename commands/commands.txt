sudo su
apt update
apt install docker.io
usermod -aG docker ubuntu
newgrp docker
wget -O- https://apt.releases.hashicorp.com/gpg | sudo gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
sudo apt update && sudo apt install terraform
curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
apt install unzip -y
unzip awscliv2.zip
sudo ./aws/install
sudo apt install curl -y
curl -LO https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl
sudo install -o root -g root -m 0755 kubectl /usr/local/bin/kubectl
mkdir supermario
cd supermario
git clone https://github.com/akshu20791/supermario-game or use this https://github.com/Elamugilan/supermario-game
cd supermario-game
cd EKS-TF
vim backend.tf and also change the main.tf(about the insatnce type) and if you are using the same root ID change the depolyment yaml
terraform init
terraform validate
terraform plan
terraform apply --auto-approve
aws eks update-kubeconfig --name EKS_CLOUD --region us-east-1(change the region based on your region)
cd ..
    kubectl apply -f deployment.yaml
kubectl apply -f service.yaml
 kubectl get all
kubectl describe service mario-service

copy the loadbalance ingress address and search in the new tab
